# Vercel: set API_SERVER_URL as project env var (used in vercel.json rewrite)
# API_SERVER_URL=your-server-domain.com

# API Base URL (optional — defaults to /api via proxy in production)
VITE_API_URL=https://your-domain.com/api

# Google Analytics Measurement ID (optional)
VITE_GA_ID=G-XXXXXXXXXX
