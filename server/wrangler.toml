name = "portfolio-samir-api"
main = "index.mjs"
compatibility_date = "2025-01-01"
compatibility_flags = [ "nodejs_compat" ]

[vars]
NODE_ENV = "production"
# SUPABASE_URL = "https://your-project.supabase.co"
# Note: SUPABASE_SERVICE_ROLE_KEY should be added via 'wrangler secret put'

# R2 Buckets (Disabled as we are using Supabase)
# [[r2_buckets]]
# binding = "BUCKET"
# bucket_name = "portfolio-samir-uploads"
